# Archive

Files here are preserved for historical context only. The current pipeline never imports from or writes to `archive/`.

- `legacy_data/`: non-canonical data fragments.
- `legacy_outputs/`: superseded experiments and duplicate compatibility outputs.
- `legacy_analysis/`: old manual spreadsheets.
- `legacy_scripts/`: reserved for retired scripts; currently empty.
